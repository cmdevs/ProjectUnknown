<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewmodel"
            type="com.cmdevs.projectunknown.ui.LoginViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvLoginTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="30sp"
            android:text="@{viewmodel.emailSignInTitle}"
            app:layout_constraintBottom_toTopOf="@+id/barrier"
            app:layout_constraintTop_toTopOf="parent"
            tool:text="로그인" />

        <androidx.constraintlayout.widget.Barrier
            android:id="@+id/barrier"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:barrierDirection="bottom"
            app:constraint_referenced_ids="tvLoginTitle"
            app:layout_constraintBottom_toTopOf="@+id/etInputEmailId"
            app:layout_constraintTop_toBottomOf="@+id/tvLoginTitle" />

        <com.cmdevs.projectunknown.ui.dialog.ClearEditTextView
            android:id="@+id/etInputEmailId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@android:color/holo_red_dark"
            android:hint="아이디를 입력해 주세요."
            android:singleLine="true"
            app:layout_constraintBottom_toTopOf="@+id/etInputEmailPw"
            app:layout_constraintTop_toBottomOf="@+id/barrier" />

        <com.cmdevs.projectunknown.ui.dialog.ClearEditTextView
            android:id="@+id/etInputEmailPw"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:backgroundTint="@android:color/holo_red_dark"
            android:hint="비밀번호 입력해 주세요."
            android:singleLine="true"
            app:layout_constraintBottom_toTopOf="@id/appCompatButton"
            app:layout_constraintTop_toBottomOf="@+id/etInputEmailId" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/appCompatButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@{viewmodel.emailSignInTitle}"
            android:textAlignment="center"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/etInputEmailPw"
            tool:text="로그인" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

